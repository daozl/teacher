#include <stdio.h>
#include <stdlib.h>

#define MAX(a,b)	((a) > (b) ? (a) : (b))

void print_s(void)
{
	printf("Hello World!\n");
}

int xx = 1;

//返回值类型 函数名（类型 形参名，类型 形参名，。。。。）
int max(int a,int b)
{
	if(a > b)
		return a;
	return b;	
}

int main()
{
	int a = 3,b = 5;
	int ret;
	
	ret = max(a,b);
	printf("%d\n",ret);

//	print_s();
	

	exit(0);
}


